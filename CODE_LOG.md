
## 2026-08-31 — SEO-описи на ВЕСЬ каталог (~6000), не лише 200 (запит SEO #1, давно спроєктовано)

Власник: «ви ж планували зробити шаблон для генератора на весь каталог, чому досі не зроблено?». Правда:
`seo_template_batch.json` згенеровано 13.08 лише на **200 SKU** (`--limit 200`) → 200 шаблонних + 30 золотих
рукописних = ті самі 230, що бачив Prom. Повна генерація так і не запущена. Зробив зараз:
`python seo_content_generator.py --export-batch seo_template_batch.json --limit 6000 --generated-at 2026-08-31`
→ **5979 шаблонних описів** (топ-6000 мінус золотий пілот). БД: `load_approved_prom_overrides` тепер **6045**
непорожніх (було 230). Генератор (`build_seo`) НЕ мінявся — стиль затверджено власником 13.08; це лише
перезапуск на повний каталог. Спот-чек: 0 порожніх, структуровані (назва+категорія+користь+бренд+країна+
розмір — ті атрибути, що EVA/ALLO читають з тексту). Детермінований (sorted by sku) → без git-churn.

**Разом із PR #451** (описи→усі площадки): тепер УСІ ~6000 карток на EVA/ALLO/Rozetka/Prom отримають
нормальний опис замість сирого Toysi. 30 золотих лишаються рукописними (пріоритет у BOOTSTRAP_BATCHES).

## 2026-08-31 — SEO-описи → усі площадки (EVA/ALLO/Rozetka), не лише Prom (запит SEO)

SEO (SEO_CHANNEL 31.08 (2)) знайшов діру: 230 затверджених SEO-описів бачив ЛИШЕ Prom
(`generate_prom_feed_top` → `seo_content_db.load_approved_prom_overrides`), а EVA/ALLO/Rozetka
беруть ІНШЕ джерело — `competitor_pricing.load_description_overrides()` → `description_overrides.json`
(лише 15 акційних). Звірив: обидва однакового формату `{sku:{"description":...}}`. Фікс (гілка
`seo-descriptions-all-platforms`): `load_description_overrides()` тепер ЗЛИВАЄ обидва — база SEO-описи
(230) + overlay акційні 15 (мають перевагу, не затираються). Best-effort (збій БД → лишається файл).
Тест: 230 SEO + 15 = 245, усі 230 SEO потрапили. **Вирішує заразом:** EVA-вимогу опису ≥30 симв. і
**мою діру 3 ALLO краще за «вивід із назви»** — ALLO читає атрибути (тип/к-сть/колір) З ТЕКСТУ опису
(бо структурованих Toysi не дає), тож якісний SEO-опис одразу дає їх обом. Prom не зачеплений (бере
SEO напряму). Наступний повний імпорт EVA/ALLO візьме багаті описи.

## 2026-08-31 — EVA: повний OOS-offer замість голого (діра 2/3 — 1608 «відсутніх полів»)

Власник підтвердив модель: 1 повний імпорт правильних карток + автоімпорт лише цін/залишків. Причина 1608
карток «відсутні бренд/фото/опис»: коли SEO робив ПОВНИЙ імпорт під акцію, фід мав **голі деактиваційні
offer'и** (name/price/stock=0/category) → повний імпорт створив OOS-товари неповними. Автоімпорт це не
лікує (він = ціни+залишки, eva.md:264). Фікс (гілка `eva-full-deactivation-offer`): деактиваційний offer
у `generate_eva_feed._build_xml` тепер емітить ще й **picture (чисте фото), vendor, description_ua** —
best-effort (чого нема в Toysi — не емітимо, деактивацію не скіпаємо). Автоімпорт зайві поля ігнорує;
наступний повний імпорт створить OOS-картку ПОВНОЮ. Тест: OOS-offer має available=false+stock=0 +
images.prom.ua-фото + бренд + опис.

Разом із #449 (чисті фото активних) фід тепер готовий до ОДНОГО чистого повного імпорту. Лишилось: діра 3
(ALLO-атрибути Тип/Кількість елементів) + відкрите EVA-питання категорії 2002.

## 2026-08-31 — EVA/ALLO: чисті фото проти «Асортимент на фото» (діра 1/3 фідів)

Власник: «чому так багато не проходять модерацію, ми не можем виконати умови площадок?». Звірив живо 3
корені (SEO-сесія 31.08 їх трасувала, я перевірив у коді). **Діра фото — найбільший важіль:** Rozetka
уникає відхилень через `_clean_pictures` (чисте головне фото images.prom.ua, без вотермарки/«асортименту»),
а **EVA (generate_eva_feed.py:960) і ALLO (generate_allo_feed.py:359) брали СИРІ `item["pictures"]` з Toysi**
(мульти-товар + вотермарки) → «Асортимент на фото».

Фікс (гілка `eva-allo-clean-photos`): новий спільний модуль **`clean_photos.py`** — `pictures_for(item, max)`
бере чисте головне фото з денного Prom-знімка (`prom_full_catalog.json` → `image_url`, апскейл 1024),
фолбек на сирі Toysi якщо товару на Prom нема (не гірше, ніж було). Підключено в емісію EVA+ALLO
(валідаційні перевірки «фото Є» лишив на сирих — коректний проксі). Rozetka не чіпав (має власну галерейну
версію). Тест: 5809 чистих фото у знімку, товар на Prom → images.prom.ua_w1024, не на Prom → Toysi-фолбек.

Лишилось (діри 2/3, окремі PR): EVA слати ПОВНИЙ offer при деактивації (не голий — не витирати картку);
ALLO змапити Toysi-дані в обов'язкові атрибути (Тип/Кількість елементів).

## 2026-08-31 — Район у comment Toysi для ВСІХ площадок (не лише EVA)

Власник уточнив: Toysi просить район для КОЖНОГО замовлення (не тільки EVA, як я спершу зробив #447).
Найуніверсальніше — в один спільний вузол: `order_router.build_toysi_order` кладе район у `comment`
(завжди йде в Toysi, видимий на формі «Автоматично: eva #…»; в окреме поле Toysi район не має, а в назву
міста класти НЕ можна — зламало б Ref-матч перевізника). Гілка `raion-all-platforms`:
- **nova_poshta.settlement_raion узагальнено** — 3 режими БЕЗ гадання: (1) точний Ref (EVA city_id);
  (2) без Ref, з `area_hint` (область) — ЛИШЕ якщо в цій обл. РІВНО ОДИН збіг за назвою (для Rozetka/Prom);
  (3) інакше "". Раніше без-Ref гілка вертала перший результат (ризик чужого району — фікс і аудит-мінору #447).
- **build_toysi_order** — район у comment для всіх НП-замовлень, best-effort (збій/неоднозначність → комент
  без району). EVA-адреси додатково несуть район у np_branch/shipping_address окремо (#447, Ref-детермінізм
  для сіл-тезок, де область не розрізняє).
- Тести: ЖИВО ref→Біляївський, Одеська(2 тезки)→"" (не гадає), build_toysi_order comment з районом + без.

## 2026-08-31 — EVA-адреса: додано РАЙОН (села-тезки в одній області + запит Toysi)

Продовження попереднього фіксу (область). Toysi-менеджер прямо попросив РАЙОН «для логістики,
перевірки з ТТН» — бо область не розрізняє села-тезки в одній обл. (Одеська: два села «Троїцьке» —
Біляївський і Любашівський р-ни). EVA назви району не дає, АЛЕ дає точний NP-Ref села (`address.city_id`).
Фікс (гілка `eva-address-raion`):
- **nova_poshta.settlement_raion(city, ref)** — район через `getSettlements.RegionsDescription`, беручи
  саме EVA-Ref (детерміновано; Ref заданий+не знайдено → "" не гадаємо). Живо звірено: 0dc0ea7c → «Біляївський».
- **_eva_delivery_address** — формат «(Xобл., Y р-н)» (best-effort: НП недоступна → сама область, як #446).
- **order_router.parse_np_branch** зроблено ДУЖКО-СВІДОМИМ (кома району всередині дужок ламала б
  `split(',')[0]`) + `_CITY_AREA_RE` допускає хвіст «, район р-н» (у area_hint лише область).
- Тести: EVA-з-районом round-trip, Rozetka-сумісність, EVA-без-району (fallback), без-дужок, порожня-область,
  + ЖИВИЙ settlement_raion 3/3 «Біляївський». Довідник eva.md оновлено.

Ручна дія по 8-080364562: Toysi — Троїцьке **Біляївського** р-ну, Одеської обл., Відділення №1.

## 2026-08-31 — EVA-адреса: додано ОБЛАСТЬ (міста-тезки не резолвились у Toysi/НП)

Власник показав форму Toysi для EVA-замовлення 8-080364562: червоне «Перевозчик не выбран! Уточняйте
область и район», місто «Троїцьке» не вибралось. Корінь у коді: `_eva_delivery_address` (orders_watcher.py)
будував адресу як `city + street.name`, **область викидав** — хоча EVA дає `region`. «Троїцьке» (село) є в
кількох областях → без області НП не розрізняє. Rozetka-версія область вписує, EVA — ні (прогалина з #206).

Фікс (гілка `eva-address-oblast`): `_eva_delivery_address` тепер вписує `region` як «(Xобл.)» — дзеркало
`_rozetka_delivery_address`, щоб `parse_np_branch` витяг area_hint і NP-резолв узяв правильне місто. Живо
звірено на 8-080364562: `region="Одеська"` (голе, без «обл.»); захист проти майбутнього «обл./область»
(re.sub, щоб не задвоїти). Тест 3/3: рядок містить «(Одеська обл.)», parse витягує область="Одеська",
+ порожня-область/задвоєння-кейси. Довідник eva.md оновлено.

Ручна дія власника по 8-080364562: на Toysi вибрати Троїцьке **Одеської** обл. (село), Відділення №1.

## 2026-08-30 — Виправлення: запит на скасування шлеться в ЧАТ Toysi (юзербот), не власнику

Власник вказав на мою помилку: тікет скасування я поставив на `send_telegram_message` (алерти власнику з ручним пересиланням), заявивши «каналу до Toysi нема». ХИБНО — `telegram_userbot_client.send_marking(text, to_toysi=...)` шле прямо в чат менеджера Toysi (Chechetenko), той самий канал, що RZ-Delivery маркування. Переписав `order_status_tracker._maybe_ticket_rozetka_cancelled`: тепер `send_marking` у чат Toysi з тим самим гейтом `MARKING_TEST_MODE` ('1' дефолт → номер власника; '0' → реальний Toysi), мітка ЛИШЕ на успіху (ретрай при UserbotError), + FYI власнику. Тест 6/6 (додано: гейт to_toysi у тест/бойовому, збій→ретрай). Довідник rozetka.md виправлено. Поставив собі memory-флаг проти вигадування архітектури без grep.

## 2026-08-30 — Rozetka: обробка скасування покупцем (pre-forward гейт + пост-forward тікет адміну)

Власник помітив на скріні кабінету Rozetka-замовлення 904664322 «Скасовано покупцем», яке ВЖЕ пішло в
Toysi. З'ясував у коді: (1) Rozetka НЕ мала pre-forward гейта проти скасування — Prom/EVA мали
(`_check_prom/eva_not_cancelled`), Rozetka ні, хоча докстрінг Prom-гейта прямо позначав це як TODO «коли
Rozetka стане активною»; (2) Toysi API взагалі не має методу скасування (лише order_create+order_status).
Власник: «якщо не можна відмінити треба автоматом відправляти у чат адміну тікет». Зробив гілкою
`rozetka-cancel-ticket`:

- **rozetka_client.py:** `ROZETKA_CANCELLED_STATUSES` = {13,17,18,28,29,32–45,49} — скасувальні ORDER-коди
  (звірено довідник rozetka.md/apidoc, 45=«Скасовано покупцем»). Навмисно БЕЗ 11/12/19 (пост-відправкові,
  їх ловить Toysi-трекер як returned).
- **order_router.py `_check_rozetka_not_cancelled`** (дзеркало Prom/EVA) — живий `get_order_status()` ПЕРЕД
  форвардом; скасувальний код → стоп + Telegram + `status='rozetka_cancelled_before_forward'` (виключено з
  `get_orders_ready_to_forward`). Fail-open (APIError/None → форвард дозволено).
- **order_status_tracker.py `_maybe_ticket_rozetka_cancelled`** — пост-форвард: замовлення вже в Toysi +
  скасоване на Rozetka → 🎫 ТІКЕТ адміну в Telegram (Toysi# + клієнт + «напиши менеджеру Toysi скасувати»),
  ідемпотентно (`rozetka_cancel_ticket_sent_at`). Додав `init_db()` у tracker's __main__ (standalone-запуск
  раніше за order_router не впаде «no such column»).
- **orders_db.py:** колонка `rozetka_cancel_ticket_sent_at` (SCHEMA + `_ensure_column` + `mark_*`),
  виключення `rozetka_cancelled_before_forward` у `get_orders_ready_to_forward`.
- Синтетичний тест (temp orders.db): 4/4 — pre-forward стоп+виключення, fail-open (6/APIError), пост-forward
  тікет+ідемпотентність, не-скасоване (61) без тікета. Довідник rozetka.md оновлено.

Замовлення 904664322 конкретно: після деплою tracker сам згенерує тікет (воно forwarded + статус 45), поки
активне. Ручна дія власника по ньому — написати Toysi скасувати, поки не відвантажили.

## 2026-08-29/30 — КОДВ-автоматизація: 5 битих Task Scheduler-задач + 2 прогалини комісій закрито

Власник передав задачу бухгалтера (видалив spawn_task-сесію): механічні КОДВ-рутини — детермінованими
скриптами, не LLM. Виявив: попередня сесія зареєструвала 5 Windows-задач під .ps1, яких не створила →
усі падали щодня (0xFFFF0000). Закрив усі 5 + 2 прогалини комісій. Усі збирачі пишуть КАНДИДАТІВ у
документи_КОДВ/, книгу НЕ пишуть (лише роль бухгалтер), крос-звірка книги READ-ONLY, кожен аудитований:

- **#426 kodv_mail_archiver.py** (→ novapay_registry_archiver.ps1) — IMAP read-only (BODY.PEEK, без Seen)
  реєстри NovaPay + акти НоваПошта → NovaPay/НоваПошта. Покрив 2 рутини. Бэклог: 10+7.
- **#427 checkbox_registry_sync.py** — Checkbox API /receipts/search → чеки-кандидати доходу + хінт
  «збігів суми в графі 2». Бэклог: 18 (8 нових ~1722₴).
- **#428 prom_commission_ledger.py** — Prom Orders API cpa_commission.amount + «частинами» + «дешева
  доставка» (§3 довідника, факт не оцінка) → графа 9. Токен order-scope у .env працює. Бэклог 606.81₴.
- **#429 graph6_daily.py** (→ graph6_daily.ps1) — собівартість реалізованих («Відвантажене») з кабінету
  Toysi «Історія замовлень» → графа 6. Сума ВКЛЮЧАЄ Збірку (§303, не донараховувати). Бэклог 14 (~2743₴).
- **#431 eva_commission_ledger.py** — фактична комісія з картки seller.eva.ua/merchant/orders/{id}
  («Сума комісії» Всього) замість 15%-оцінки (прогалина бухгалтера). Бэклог 10 (398.89₴, 8 не в книзі).
- **#430 KODVDailyCheck прибрано** — редундантна (Prom-дохід-детектор перекрито крос-звіркою #428).
- **#432 PrivatDailySync прибрано** — структурно нездійсненна: Privat Автоклієнт API 401 (платний тариф),
  лише вручну Приват24. bank_check.py готовий на випадок тарифу.

Уроки (у пам'ять): читати recall+довідники ПЕРЕД побудовою (не reverse-engineer наживо); дубль-варта
recall-guard тепер additionalContext (агенту), не ask (власнику); методологія комісій — у §3/§EVA/§Графа6
довідника, звірено живо. Prom-токен «протухає» від простою й оживає від використання (не products-only).
